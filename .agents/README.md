# Repo-Local Codex Assets

This folder exists so Codex can discover repo-local skills using the current OpenAI guidance.

- Skills live in `.agents/skills/`
- Each skill is a directory with `SKILL.md` and optional `scripts/`, `references/`, `assets/`, and `agents/openai.yaml`

Treat this folder as reusable Codex infrastructure, not product code.
