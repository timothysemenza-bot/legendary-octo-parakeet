# Workspace

This folder is now a legacy local scratch area.

It is intentionally not part of the new active project structure.

Use it only for volatile local items that are already tied to this path, such as:

- temporary browser profiles
- transient frame captures
- local scratch notes that are not ready to become project work

New active work should go into `projects/`, `outputs/`, or the focused product roots instead.
